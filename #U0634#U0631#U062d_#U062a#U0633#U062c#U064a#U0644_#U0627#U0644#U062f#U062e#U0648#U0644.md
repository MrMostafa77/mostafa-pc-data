# إزاي تفعّل شاشة الدخول (Firebase Authentication)

الموقع دلوقتي بقى فيه شاشة دخول (يوزر + باسورد) قبل ما حد يقدر يشوف
أو يعدّل بياناتك، وده حماية حقيقية عن طريق Firebase مش مجرد إخفاء
للواجهة.

## الخطوات (مرة واحدة بس)

### 1) فعّل طريقة الدخول في Firebase Console
1. روح على [Firebase Console](https://console.firebase.google.com/) → مشروعك `pc-data-mostafa`.
2. من القائمة الجانبية: **Build → Authentication**.
3. دوس **Get started** لو أول مرة.
4. من تبويب **Sign-in method** فعّل **Email/Password** (Enable ثم Save).

### 2) اعمل يوزر بتاعك
1. برضو من **Authentication** روح تبويب **Users**.
2. دوس **Add user**.
3. **الإيميل**: اكتب اسم المستخدم اللي هتدخل بيه في الموقع + `@gamevault.local`
   في الآخر. مثال: لو عايز تدخل بيوزر اسمه `mostafa`، اكتب الإيميل:
   `mostafa@gamevault.local`
4. **الباسورد**: اكتب أي باسورد (6 حروف على الأقل)، وده هو نفسه اللي هتكتبه
   في شاشة الدخول.
5. دوس **Add user**.

> ملحوظة: `@gamevault.local` مش إيميل حقيقي وملوش أي علاقة بجيميل أو
> أي حاجة — Firebase بس بيحتاج شكل إيميل (حاجة@حاجة)، فده حل بسيط
> عشان تكتب "يوزر" بس في الشاشة من غير ما تحتاج إيميل حقيقي. لو
> عايز تضيف أكتر من يوزر (مثلاً لأفراد تانيين)، كرر نفس الخطوات
> بإيميلات مختلفة.

### 3) حدّث قواعد Firestore
1. من **Build → Firestore Database → Rules**.
2. امسح اللي موجود وحط بالظبط محتوى ملف `firestore.rules` الموجود
   في المشروع ده (اتحدث تلقائيًا عشان يمنع أي حد مش مسجل دخول من
   القراءة أو الكتابة).
3. دوس **Publish**.

### 4) ارفع الملفات المحدثة
ارفع كل ملفات المشروع (زي ما بتعمل عادي على GitHub Pages أو أي
استضافة) — أهم حاجة `index.html`، `css/style.css`، `js/auth-gate.js`،
و`js/cloud-sync.js` اتغيروا.

## بعد كده
- افتح الموقع → هتلاقي شاشة دخول باسم مستخدم وكلمة مرور.
- سجّل دخول بنفس اليوزر والباسورد اللي عملتهم في الخطوة 2 (اكتب
  `mostafa` بس، مش الإيميل الكامل).
- تسجيل الدخول بيفضل محفوظ في المتصفح، مش هتحتاج تدخله كل مرة.
- في زرار 🚪 جنب زرار الوضع الليلي/النهاري في الأعلى لو حبيت تسجل خروج.

## لو نسيت الباسورد
من **Authentication → Users** في Firebase Console، تقدر تعمل **Reset
password** أو تمسح اليوزر وتعمل واحد جديد.
