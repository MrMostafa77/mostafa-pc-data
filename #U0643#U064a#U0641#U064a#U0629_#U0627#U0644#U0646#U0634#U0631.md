# كيفية نشر الموقع أونلاين + تفعيل الحفظ من أي جهاز

## اللي اتغيّر بالظبط في ملفاتك
- ملف جديد: `js/cloud-sync.js` (طبقة مزامنة عامة للبيانات + مزامنة صور الأغلفة عبر Firestore)
- ملف جديد: `js/cover-store.js` (تخزين صور الأغلفة محليًا في IndexedDB بدل localStorage المحدود المساحة)
- ملف جديد: `firebase-config.js` (إعدادات فاضية تعبّيها انت)
- تعديلات في `index.html`: إضافة سكريبتات Firebase (app, firestore) و`cover-store.js`، ثم تحميل طبقة المزامنة بدل ما يحمّل `js/data-inline.js` و`js/app.js` مباشرة (وهي اللي بتحمّلهم بعدين بنفس الترتيب)
- تعديل بسيط في `js/app.js`: طريقة حفظ/حذف صور الأغلفة بقت بتمر على IndexedDB + المزامنة السحابية بدل الحفظ المباشر في localStorage (نفس الشكل والاستخدام تمامًا من واجهة المستخدم)

النتيجة: الموقع شغال بنفس الشكل والتصميم والميزات 100%، والحفظ (بيانات + صور) بقى يشتغل من أي جهاز بدل ما يكون محصور بمتصفح واحد، مجانًا بالكامل وبدون أي بطاقة ائتمان (مفيش استخدام لـ Firebase Storage نهائيًا، الصور بتتقسم لأجزاء صغيرة وتتخزن في Firestore نفسه).

---

## الخطوة 1: قاعدة بيانات مجانية (Firebase) — 5 دقائق

1. https://console.firebase.google.com → سجّل دخول بحساب Google
2. **Add project** → اسم مثل `mostafa-pc-data` → أكمل (تقدر تلغي Analytics) → **Create**
3. من القائمة الجانبية: **Build → Firestore Database → Create database**
4. اختر **Start in test mode** → اختر أقرب سيرفر → **Enable**
5. ارجع لصفحة المشروع → اضغط أيقونة **</> (Web)** → سمّه `gamevault-web` → **Register app**
6. انسخ القيم اللي هتظهر (apiKey, authDomain...) وحطها في ملف `firebase-config.js` مكان القيم الوهمية، واحفظ الملف

### تأمين القاعدة (بعد التجربة الأولى)
روح **Firestore Database → Rules** واستبدل المحتوى بـ:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gamevault/{doc} {
      allow read, write: if true;
    }
    match /gamevault_store/{key} {
      allow read, write: if true;
      match /chunks/{chunkId} {
        allow read, write: if true;
      }
    }
    match /covers/{gameId} {
      allow read, write: if true;
      match /chunks/{chunkId} {
        allow read, write: if true;
      }
    }
  }
}
```
(فتح كامل بدون تسجيل دخول — مناسب لموقع شخصي. لو حبيت حماية بكلمة مرور، قولّي. مجموعة `covers` دي هي اللي بتتخزن فيها صور الأغلفة المرفوعة، ومجموعة `gamevault_store` (جديدة) هي اللي بقى بيتخزن فيها كل بيانات التطبيق (Library, Game Dates, إلخ) بدل المستند القديم — **الاتنين لازم يكونوا في الـ Rules زي ما هو فوق بالظبط، وإلا هتفشل كل عمليات الحفظ برسالة "Missing or insufficient permissions".**)

**ملحوظة:** لو مشروعك على خطة **Spark (المجانية)** — وده الافتراضي — كل ده هيشتغل من غير أي بطاقة ائتمان أو أي دفع. الحد المجاني لـ Firestore هو 1 جيجابايت تخزين و20-50 ألف عملية قراءة/كتابة يوميًا، وده أكتر من كافي جدًا لمكتبة ألعاب شخصية حتى مع صور كتير.

---

## الخطوة 2: النشر على GitHub Pages (رابط دائم ومجاني)

1. https://github.com → سجّل حساب لو مالكش
2. **New repository** → اسم مثل `mostafa-pc-data` → **Public** → **Create repository**
3. اضغط **uploading an existing file** → اسحب كل الملفات والمجلدات (index.html, css/, js/, data/, assets/, firebase-config.js، إلخ) بنفس الأسماء والهيكل
4. **Settings → Pages** → تحت **Branch** اختر `main` والمجلد `/root` → **Save**
5. بعد دقيقة أو اثنتين هيظهر رابط زي:
   `https://<اسم_حسابك>.github.io/mostafa-pc-data/`

هذا هو رابط موقعك الدائم — يفتح من أي كمبيوتر أو جوال.

---

## الخطوة 3: جرّب المزامنة

1. افتح الرابط من الكمبيوتر → أضف لعبة جديدة أو ارفع صورة غلاف للعبة
2. افتح نفس الرابط من الجوال → هتلاقي التعديل والصورة ظاهرين (قد ياخد كذا ثانية حسب حجم الصورة)
3. لو عدّلت من الجوال، ارجع حدّث صفحة الكمبيوتر → هتلاقي تعديل الجوال وصل

لو ملف firebase-config.js لسه فيه القيم الوهمية (يعني ما عملتش الخطوة 1)، الموقع هيشتغل عادي جدًا بس بحفظ محلي لكل جهاز — من غير أي مشاكل.
